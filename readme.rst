#Create a folder in the root of your Web Server (Apache) 
#paster all files (inculding .htaccess) into the folder
#Create database techno
#upload techno.sql into the empty DB
#go to "hostname"/techno on your browser
